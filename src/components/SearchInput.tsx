import React from "react";
import { View, TextInput } from "react-native";

const SearchInput = () => {
  return (
    <View>
      <TextInput></TextInput>
    </View>
  );
};

export default SearchInput;
